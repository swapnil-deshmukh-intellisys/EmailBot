{
  "name": "dashboard-next",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "kill:next": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"& { $procs = Get-CimInstance Win32_Process -Filter \\\"Name='node.exe'\\\" | Where-Object { $_.CommandLine -like '*dashboard-next*next*dev*' -or $_.CommandLine -like '*dashboard-next*start-server.js*' }; $procs | ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue } }\"",
    "kill:3000": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"& { $owners = Get-NetTCPConnection -LocalPort 3000 -State Listen -ErrorAction SilentlyContinue | Select-Object -ExpandProperty OwningProcess -Unique; foreach ($processId in $owners) { Stop-Process -Id $processId -Force -ErrorAction SilentlyContinue } }\"",
    "clean:next": "node -e \"const fs=require('fs'); for (const dir of ['.next','.turbo']) { fs.rmSync(dir,{recursive:true,force:true}); }\"",
    "dev:next": "next dev -p 3000",
    "dev": "npm run kill:next && npm run kill:3000 && npm run clean:next && npm run dev:next",
    "dev:clean": "npm run dev",
    "build:next": "next build",
    "build": "npm run clean:next && npm run build:next",
    "start": "next start",
    "worker:campaigns": "node scripts/campaign-worker.mjs",
    "cleanup:legacy": "node scripts/cleanup-legacy-data.mjs",
    "lint": "next lint"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "imapflow": "^1.2.18",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "next": "^14.2.5",
    "nodemailer": "^6.10.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.5"
  }
}
